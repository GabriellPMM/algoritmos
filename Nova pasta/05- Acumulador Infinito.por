programa {
  funcao inicio() {
    inteiro numero=1, soma=0
      enquanto(numero != 0){
        escreva("digite um número: ")
        leia(numero)
        soma = soma+numero
      }
    escreva(soma)

  }
}
